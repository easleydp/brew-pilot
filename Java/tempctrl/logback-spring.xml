<configuration scan="true">
    <include resource="org/springframework/boot/logging/logback/base.xml"/>
    <!-- Only uncomment on server (unless testing):
    <springProperty name="smtpHost" source="spring.mail.host" />
    <springProperty name="smtpPort" source="spring.mail.properties.mail.smtp.port" />
    <springProperty name="smtpTo" source="mail.to" />
    <springProperty name="smtpFrom" source="mail.from" />
    <springProperty name="smtpUsername" source="spring.mail.username" />
    <springProperty name="smtpPassword" source="spring.mail.password" />
    <appender name="EMAIL" class="ch.qos.logback.classic.net.SMTPAppender">
        <smtpHost>${smtpHost}</smtpHost>
        <smtpPort>${smtpPort}</smtpPort>
        <to>${smtpTo}</to>
        <from>${smtpFrom}</from>
        <username>${smtpUsername}</username>
        <password>${smtpPassword}</password>
        <subject>BrewPilot: %logger{20} - %m</subject>
        <STARTTLS>true</STARTTLS>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>%date %-5level %logger{35} - %message%n</pattern>
        </layout>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
    </appender>
    <root level="DEBUG">
        <appender-ref ref="EMAIL" />
    </root>
    -->
</configuration>